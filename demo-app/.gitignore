# Secrets — NEVER commit API keys
.env
.env.local
.env*.local
*.key

# Dependencies & build output
node_modules/
.next/
out/

# macOS
.DS_Store

# Eval reports — generated locally from real (paid) API calls, not for commit
evals/report.md
